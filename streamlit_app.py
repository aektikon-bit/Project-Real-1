import streamlit as st

st.set_page_config(page_title="คำนวณเลขง่ายๆ", layout="centered")
st.title("🧮 คำนวณเลขง่ายๆ")

# ใช้ number_input แทน input()
a = st.number_input("กรอกตัวเลขแรก", value=0.0)
b = st.number_input("กรอกตัวเลขที่สอง", value=0.0)

# ปุ่มกดคำนวณ
if st.button("คำนวณ"):
    st.write("### ผลลัพธ์")
    st.write("ผลบวก:", a + b)
    st.write("ผลลบ:", a - b)
    st.write("ผลคูณ:", a * b)
    if b == 0:
        st.write("ผลหาร: ไม่สามารถหารด้วยศูนย์ได้")
    else:
        st.write("ผลหาร:", a / b)
